import React, { useEffect } from 'react'

import useFetch from '../hooks/useFetch'

const PopularTags = () => {
	/* const [{response, isLoading, error}, doFetch] = useFetch('/tags')

	  useEffect(() => {
		doFetch()
	}, [doFetch])
	  */
	return <div>{/* <b>PopularTags</b>
	<br/>welcome<br/>
		implementations<br/>
		codebaseShow<br/>
		introduction */}
	</div>
}

export default PopularTags